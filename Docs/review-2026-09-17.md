# Physics and jumping review - 2026-09-17

Reviewed the uncommitted stairs/jumping implementation, shared collision and room
parser, Unity authoring/native integration, Simulant input/presentation, test
fixtures, build wiring, and documentation against AGENTS.md. No blocking gameplay
finding remains in this review.

The shared Game directory remains free of Unity, Simulant and KOS dependencies.
The v3 upright character uses bounded static-box sweeps, gravity and ground-only
jumping. V1/v2 preserve the original flat controller. Storage is fixed, movement
allocates no memory, and long frames are capped to six movement substeps. Unity
is an authoring/presentation host; production 3D source remains Blender.

## Review corrections

- Prop-bundle regression assumed the active export always contained AC textures.
  It now validates the selected export and uses a separate minimal textured
  fixture for corrupt/missing texture and stale-file checks.
- Added explicit Space and controller B press/hold/release checks in Simulant,
  and controller east-button press/hold checks in Unity. The Play-mode test
  exercises actual Update/LateUpdate and a held Space through landing.
- Corrected stale default-room/control documentation, normalized document text
  encoding, and excluded Python test caches from Git.

## Validation

- Full native regression suite with AddressSanitizer: passed.
- Audio and prop bundle staging/corruption tests: passed.
- Complete Unity runner: passed (architecture, original key/door room, audio,
  ranges, trimming, props, lighting, stairs and live keyboard/jump tests).
- Simulant Linux build and headless input/stairs/gap-jump playback: passed.
- The preceding jumping build produced the Dreamcast CDI and passed its directory
  and asset checks. Review edits affect tests/documentation only, not its runtime.
- Original SampleScene remains unchanged. The runner restores the selected v3
  bundle after testing the original room. Git whitespace checks pass.

## Remaining scope and evidence limits

Static axis-aligned boxes only; no slopes, dynamic bodies or moving platforms.
Ladder teleport/climbing is deferred. Actual Dreamcast performance still needs
hardware profiling. The intermittent Unity keyboard report cleared after reopening
Unity; its original cause was not reproduced, and is not claimed fixed. The
editor status display and real Play-mode input test remain for diagnosis.
